<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, input_html: {class: 'photo-upload'} %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit class: 'btn btn-success btn-lg w-100' %>
<% end %>

